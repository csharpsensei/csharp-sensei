// NOT a wide event — 3 rows, one story, reassembled later
logger.LogInformation("Auth ok {TraceId}", traceId);
logger.LogInformation("Cart loaded {TraceId}", traceId);
logger.LogInformation("Payment declined {TraceId}", traceId);
