// Two failures, two shapes.
//
//   SMS      throws      loud, found within the hour
//   Audit    lies        quiet, found by a customer
//
// Neither is a compiler error. Both overrides match the
// signature exactly, so both of them build.
