namespace BuilderPattern.Rooms;

/// <summary>
/// The three room types this hotel sells. Kept deliberately small: the lesson
/// is about how a booking is constructed, not about the hotel.
/// </summary>
public enum RoomType
{
    Single,
    Double,
    Family
}
