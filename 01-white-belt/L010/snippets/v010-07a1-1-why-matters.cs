// Before
private readonly FileDestination _destination;

// After — one word
private readonly IReportDestination _destination;

// The object is the same object.
// The class no longer knows which one it is.
