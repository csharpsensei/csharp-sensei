// The class names the exact type it talks to.
private readonly FileDestination _destination
    = new FileDestination("C:/reports");

// Not "something I can send a report to".
// A file saver. Specifically. By name.
// The compiler will hold it to that.
